# Python dependencies for UWD
#
# Installs the backend in editable mode + optional visualization deps (Streamlit/Plotly)
-e ./backend[viz]

# Trade surveillance prototype (this repo root)
# Installs numpy/pandas/pyarrow/scikit-learn/streamlit/plotly via pyproject.toml
-e .

# Dev/test tooling for the prototype
pytest>=8.2
