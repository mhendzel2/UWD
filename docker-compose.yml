services:
  db:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_USER: uw_app
      POSTGRES_PASSWORD: uw_password
      POSTGRES_DB: uw_eod
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:
    driver: local
